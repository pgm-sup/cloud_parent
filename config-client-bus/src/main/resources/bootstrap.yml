spring:
  cloud:
    config:
      discovery:
        service-id: config-server-bus
        enabled: true
      name: config-client
      profile: dev

eureka:
  client:
    serviceUrl:
      defaultZone: http://admin:123456@localhost:8761/eureka/
