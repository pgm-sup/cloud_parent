spring:
  cloud:
    config:
      discovery:
        service-id: config-server-bus
        enabled: true
      name: gateway-server
      profile: dev

eureka:
  client:
    serviceUrl:
      defaultZone: http://admin:123456@localhost:8761/eureka/ # 注册中心地址